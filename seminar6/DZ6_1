# Задайте список из нескольких чисел. Напишите программу,
#  которая найдёт сумму элементов списка, стоящих на нечётной позиции.

# list =[1, 2, 3, 4, 5]
# sum = 0
# for i in range(len(list)):
#     print(i)
#     if i%2 != 0:
#         sum +=list[i]
# print(sum)

#Вариант с List Comprehension
#В таком виде  не работает 
# ( складываются нечетные числа, а не позиции,) 
#так и не придумал как сложить не четные позиции в таком формате
#если не сложно, напишите ,пожалуйста, в коментарии возможный вариант решения в таком случае


list =[1, 2, 3, 4, 5]
list1 = [i for i in range(len(list) + 1) if i%2!= 0]
print(sum(list1))

